[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "exsys-hub"
version = "1.0.0"
dependencies = ["pyserial", "pyyaml"]

[tool.setuptools.packages.find]
include = ["exsys_hub*"]
